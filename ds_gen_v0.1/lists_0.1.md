






----------

Public methods:

get(index) returns item
set(index, item)
insert(index, item)
delete(index)
contains(item) returns bool
push(item)

Private methods:

insertNodeAfter(node, other)
deleteNode(node)
